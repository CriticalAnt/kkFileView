# 复制为 deploy/.env 后填入真实值(该文件已被 .gitignore 排除,不会入库)

# 预览服务对外地址:浏览器实际访问 kkFileView 用的地址,反代时填外部域名
KK_BASE_URL=http://<你的服务器IP或域名>:<端口>

# 信任站点白名单:预览源文件所在的主机,多个用逗号分隔;留空则回退为 *(不推荐)
KK_TRUST_HOST=<预览源文件主机,如对象存储IP或域名>
